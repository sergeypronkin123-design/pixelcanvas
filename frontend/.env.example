# API base URL (leave empty for same-origin proxy in dev)
VITE_API_URL=

# WebSocket URL (leave empty for same-origin proxy in dev)
VITE_WS_URL=

# Stripe publishable key (loaded from backend /api/config in production)
VITE_STRIPE_KEY=pk_test_xxx
